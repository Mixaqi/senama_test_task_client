import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    paddingHorizontal: 8,
    paddingVertical: 4,
    minWidth: 120,
    marginRight: 6,
    borderRadius: 4,
    borderColor: '#434343',
  },
});
